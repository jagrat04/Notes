import { Link } from "react-router-dom";

const initialHome = () => {
  return (
    <div
      style={{
        backgroundColor: 'blue',
        width: '100px',
        height: '100px'
      }}
    />
  );
};

export default initialHome ;
